type Props = {
  children: React.ReactNode;
  href: string;
  isExtended?: boolean;
};

export default function SidebarLink({ children, isExtended, href }: Props) {
  return <div>SidebarLink</div>;
}
